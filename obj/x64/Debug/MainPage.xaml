<Page
    x:Class="App1.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="6*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title Typing Area -->
        <TextBox HorizontalAlignment="Stretch" Grid.Row="1" Grid.Column="1" />

        <!-- Settings Area -->
        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Quantity Area: Videos or Playlists -->
            <StackPanel Margin="10">
                <RadioButton x:ConnectionId='5' Content="Videos" GroupName="QuantityType" Tag="Videos"
                                                                   IsChecked="True"/>
                <RadioButton x:ConnectionId='6' Content="Playlists" GroupName="QuantityType" Tag="Playlists"
                                                                  />
            </StackPanel>

            <!-- Quality Slider -->
            <Slider x:ConnectionId='1' x:Name="qualitySlider" Header="Quality" Grid.Column="1"  Margin="10"
                    Value="100"                                      />

            <!-- Media Type Area: Video or Audio -->
            <StackPanel Grid.Column="2" Margin="10">
                <RadioButton x:ConnectionId='3' Content="Video" GroupName="MediaType" Tag="Video"
                                                               />
                <RadioButton x:ConnectionId='4' Content="Audio" GroupName="MediaType" Tag="Audio"
                                                                IsChecked="True"/>
            </StackPanel>

            <!-- Wait for Selection CheckBox -->
            <CheckBox x:ConnectionId='2' x:Name="waitForSelectionCheckBox" Content="Wait for my selection"
                      Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="2"
                      IsChecked="False"                                                                       />
        </Grid>

    </Grid>
</Page>

